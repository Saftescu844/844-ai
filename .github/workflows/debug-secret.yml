name: Debug Secret Hash
on:
  workflow_dispatch:
jobs:
  hash:
    runs-on: ubuntu-latest
    steps:
      - name: Print hash of DATABASE_URL
        run: printf '%s' "$DATABASE_URL" | sha256sum
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}