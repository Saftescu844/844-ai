name: Database Backup 844-ai.ro

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch: {}

jobs:
  backup:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Instalează postgresql-client 17
        run: |
          sudo apt-get update
          sudo apt-get install -y postgresql-common
          sudo /usr/share/postgresql-common/pgdg/apt.postgresql.org.sh -y
          sudo apt-get install -y postgresql-client-17

      - name: Verifică versiunea pg_dump instalată
        run: |
          which pg_dump
          pg_dump --version
          ls -la /usr/lib/postgresql/*/bin/pg_dump 2>/dev/null || echo "niciun binar versionat găsit"

      - name: Creează backup
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          TIMESTAMP=$(date -u +%Y%m%d_%H%M%S)
          /usr/lib/postgresql/17/bin/pg_dump "$DATABASE_URL" -F c -f "backup_${TIMESTAMP}.dump"
          ls -lh backup_${TIMESTAMP}.dump

      - name: Încarcă backup ca artifact
        uses: actions/upload-artifact@v4
        with:
          name: db-backup-${{ github.run_id }}
          path: "*.dump"
          retention-days: 90
